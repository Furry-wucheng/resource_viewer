# 阶段 02 — 数据层与基础设施

> **定位**：Foundation | **子阶段数**：9 | **前置依赖**：01（项目脚手架与基础 UI）
> **目标**：完成 drift 数据库建表、freezed 领域模型、Repository 实现和错误处理基础设施

---

## 阶段总体目标

交付完整的数据库 Schema、领域模型层和 Repository 层，以及统一的错误处理框架（DomainError 类型树、Result\<T\>、UiState、Progress\<T\>）。此阶段完成后，所有数据通路就绪，ViewModel 可直接注入 Repository 获取数据。

## 参考文档

| 类型 | 文档 |
|------|------|
| 数据模型 | `@prd/05-数据模型.md` |
| 数据库 DDL | `@tech/04-数据库设计.md` |
| 架构分层 | `@tech/02-架构设计.md` §1~§3 |
| 错误处理 | `@tech/05-错误处理策略.md` |
| 测试策略 | `@tech/06-测试策略.md` §3（drift 内存库）§4.1~§4.2 |

---

## 2.1 枚举定义

### 执行目标
定义三个核心枚举类型，供数据库表和领域模型共用。

### 任务边界
- 纯 Dart 枚举，无数据库依赖
- `SourceType` 预留 ftp/webdav（架构预留）

### 产出物
| 文件 | 说明 |
|------|------|
| `lib/data/models/enums.dart` | `SourceType`, `ResourceType`, `OrganizationMode` 枚举 |

### 验收标准
- [ ] `SourceType { local, smb, ftp, webdav }`
- [ ] `ResourceType { folder, pdf, archive }`
- [ ] `OrganizationMode { direct, chapter, flatgrid, gallery }`
- [ ] `flutter analyze` 通过

---

## 2.2 drift 数据库初始化

### 执行目标
创建 `AppDatabase` 类，配置后台 Isolate + WAL + foreign_keys，实现迁移策略骨架。

### 任务边界
- 表暂为空（2.3~2.6 填充）
- 仅负责连接管理、PRAGMA、迁移策略框架

### 产出物
| 文件 | 说明 |
|------|------|
| `lib/data/services/database_service.dart` | `AppDatabase` + `_openConnection()` |

### 验收标准
- [ ] `NativeDatabase.createInBackground()` 正确使用
- [ ] `setup` 回调中执行 `PRAGMA journal_mode = WAL;` `PRAGMA foreign_keys = ON;` `PRAGMA synchronous = NORMAL;`
- [ ] `readPool: 3` 配置
- [ ] `MigrationStrategy` 含 `beforeOpen`（重设 foreign_keys + 补建内置标签）和 `onCreate` 钩子
- [ ] 单元测试：`NativeDatabase.memory()` 创建成功，PRAGMA 生效

---

## 2.3 sources 表定义

### 执行目标
按 `@tech/04-数据库设计.md` §4.1 定义 `sources` drift 表。

### 任务边界
- 仅表定义，DAO 查询在 2.8 实现
- `passwordStored` 列，不存密码

### 产出物
| 文件 | 说明 |
|------|------|
| `lib/data/models/sources.dart` | `Sources` drift 表 + `SourceType` 枚举 |

### 验收标准
- [ ] 13 列完整定义（id/name/type/rootPath/host/port/username/passwordStored/domain/enabled/isAvailable/lastCheckAt/createdAt/updatedAt）
- [ ] `type` 使用 `textEnum<SourceType>()`
- [ ] `passwordStored` 默认 `false`
- [ ] 单元测试：建表 + 插入一条 source 记录成功

---

## 2.4 resources 表定义

### 执行目标
按 `@tech/04-数据库设计.md` §4.2 定义 `resources` drift 表。

### 任务边界
- 含 Unique 约束 `(sourceId, relativePath)` 和复合索引

### 产出物
| 文件 | 说明 |
|------|------|
| `lib/data/models/resources.dart` | `Resources` drift 表 + `ResourceType`/`OrganizationMode` 枚举 |

### 验收标准
- [ ] 13 列完整定义
- [ ] FK `sourceId references Sources onDelete cascade`
- [ ] Unique `(sourceId, relativePath)` 约束
- [ ] 索引：`(createdAt, id)` 和 `(name, id)` 复合索引
- [ ] `organizationMode` nullable（null = 未判定）
- [ ] 单元测试：级联删除（删 source → resource 跟随删除）

---

## 2.5 tags 表定义

### 执行目标
按 `@tech/04-数据库设计.md` §4.3 定义 `tags` drift 表。

### 任务边界
- 唯一约束 `name`
- `isBuiltIn` 列区分内置/自定义

### 产出物
| 文件 | 说明 |
|------|------|
| `lib/data/models/tags.dart` | `Tags` drift 表 |

### 验收标准
- [ ] 6 列完整定义（id/name/color/isBuiltIn/createdAt/updatedAt）
- [ ] `name` UNIQUE，`maxLength: 20`
- [ ] `color` `maxLength: 7`
- [ ] 单元测试：tags 表插入 + 同名冲突抛异常

---

## 2.6 resource_tags 表定义

### 执行目标
按 `@tech/04-数据库设计.md` §4.4 定义 `resource_tags` 多对多关联表。

### 任务边界
- 联合 PK `(resourceId, tagId)`
- 手动添加 `(tagId, resourceId)` 索引

### 产出物
| 文件 | 说明 |
|------|------|
| `lib/data/models/resource_tags.dart` | `ResourceTags` drift 表 + 手动索引 SQL |

### 验收标准
- [ ] Joint PK `(resourceId, tagId)`
- [ ] 两个 FK 均 `onDelete cascade`
- [ ] `@TableIndex.sql` 手动创建 `CREATE INDEX idx_rt_tag_resource ON resource_tags(tag_id, resource_id)`
- [ ] 单元测试：级联删除（删 resource → resourceTag 跟随；删 tag → resourceTag 跟随）

---

## 2.7 内置标签播种

### 执行目标
实现数据库创建时自动播种"收藏"内置标签（固定 UUID），以及启动时补建逻辑。

### 任务边界
- 仅"收藏"一个内置标签，`isBuiltIn=true`, `color=#FFC107`

### 产出物
| 位置 | 说明 |
|------|------|
| `database_service.dart` | `_seedBuiltInTags()` + `_ensureBuiltInTags()` 方法 |

### 验收标准
- [ ] `onCreate` 中自动插入"收藏"标签，UUID = `00000000-0000-0000-0000-000000000001`
- [ ] `beforeOpen` 中检测内置标签是否丢失，丢失则补建
- [ ] 单元测试：内存库建表后 tags 表含"收藏"标签；手动删除后重开库自动补建

---

## 2.8 freezed 领域模型

### 执行目标
按 `@prd/05-数据模型.md` 定义 5 个 freezed 领域模型（Source/Resource/Tag/FileEntry/Chapter），实现 JSON 序列化。

### 任务边界
- 与 drift 表字段一一对应，但不可变（freezed）
- `FileEntry` 为跨源统一文件条目
- `Chapter` 为章节模式下的章节模型

### 产出物
| 文件 | 说明 |
|------|------|
| `lib/domain/models/source.dart` | Source freezed model |
| `lib/domain/models/resource.dart` | Resource freezed model |
| `lib/domain/models/tag.dart` | Tag freezed model |
| `lib/domain/models/file_entry.dart` | FileEntry freezed model（统一跨源文件条目） |
| `lib/domain/models/chapter.dart` | Chapter freezed model（章节名 + 路径 + 封面路径 + 页数） |

### 验收标准
- [ ] 5 个模型均 `@freezed` 注解，`fromJson`/`toJson` 可用
- [ ] `copyWith` 正确工作
- [ ] `==`/`hashCode` 按业务语义定义
- [ ] 单元测试：每个模型的序列化往返测试 + copyWith 测试
- [ ] `flutter analyze` 通过，`build_runner` 代码生成无报错

---

## 2.9 错误处理基础设施

### 执行目标
按 `@tech/05-错误处理策略.md` 实现 DomainError 类型树、Result\<T\>、UiState、Progress\<T\>、BaseViewModel。

### 任务边界
- 纯 Dart 基础设施，不依赖 Flutter
- Repository 错误目前抛出原始异常（阶段 03+ 包装为 Result）

### 产出物
| 文件 | 说明 |
|------|------|
| `lib/domain/models/domain_error.dart` | `DomainError` sealed class + 7 个子类 |
| `lib/domain/models/result.dart` | `Result<T>` sealed class（`Ok` / `Err`） |
| `lib/domain/models/progress.dart` | `Progress<T>` sealed class（`ProgressUpdate` / `ProgressDone` / `ProgressError`） |
| `lib/ui/core/view_models/base_view_model.dart` | `BaseViewModel` 抽象类（state/errorMessage/lastError/canRetry） |

### 验收标准
- [ ] `DomainError` sealed class 含 7 个子类型：`SourceUnreachableError`, `SourceAuthError`, `FileNotFoundError`, `FileAccessDeniedError`, `NetworkTimeoutError`, `MediaLoadError`, `MediaEncryptedError`, `ValidationError`, `DatabaseError`
- [ ] `Result<T>` 含 `Ok(value)` 和 `Err(error)`
- [ ] `Progress<T>` 含 `ProgressUpdate(value, current, total)`, `ProgressDone(result)`, `ProgressError(error)`
- [ ] `BaseViewModel` 含 `state` (UiState), `errorMessage`, `lastError`, `canRetry`, `_startLoading()`, `_setResult()`, `retry()`
- [ ] `canRetry` 逻辑：`SourceUnreachableError`/`FileNotFoundError`/`NetworkTimeoutError`/`MediaLoadError` 返回 true，其余 false
- [ ] `_mapError` 中文文案映射完整
- [ ] 单元测试：每种 Error 类型的 canRetry 判定，Result 的 switch 穷举
