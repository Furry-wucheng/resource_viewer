# 阶段 03 — 本地源链路

> **定位**：Core | **子阶段数**：23 | **前置依赖**：01（脚手架）, 02（数据层）<br>**注**：§3.21~3.23 为新增 P0 缺口子阶段
> **目标**：首个端到端用户链路 — 添加本地文件夹 → 扫描入库 → 首页缩略图网格 → 查看器翻页/视频播放

---

## 阶段总体目标

交付从"添加本地文件夹数据源 → 文件浏览器勾选 → 扫描为 Resource → 首页缩略图网格展示 → 点击进入查看器翻页/播放视频"的完整闭环。此阶段完成后，用户可用本地文件夹进行完整的收藏-浏览体验。

## 参考文档

| 类型 | 文档 |
|------|------|
| 需求 | `@prd/01-资源库首页.md`（首页网格/缩略图/空状态） |
| 需求 | `@prd/03-数据源管理.md`（本地源/文件浏览器/多选/批量添加） |
| 需求 | `@prd/04-资源查看器.md`（统一查看器/滑动条/加载失败/视频播放器） |
| 原型 | `@design/homepage.html`, `@design/file-browser.html`, `@design/viewer.html`, `@design/source-list.html` |
| 架构 | `@tech/02-架构设计.md` §4.1（FileSource）§4.2（ContentProvider）§4.4（ThumbnailGenerator） |
| 性能 | `@tech/03-性能优化设计.md` §3（首页网格）§4（查看器翻页）§7（缩略图 LRU）§8（扫描）§9（内存管理） |

---

## 3.1 FileSource 抽象接口

### 执行目标
定义 `FileSource` 抽象接口，统一本地/SMB 的文件访问 API。

### 任务边界
- 仅接口定义，不实现
- 接口方法覆盖：`listDirectory`, `stat`, `readFile`, `streamFile`, `testConnection`, `disconnect`

### 产出物
| 文件 | 说明 |
|------|------|
| `lib/shared/file_source/file_source.dart` | `abstract class FileSource` |

### 验收标准
- [x] 接口定义与 `@tech/02-架构设计.md` §4.1 一致
- [x] 含 `sourceId` getter
- [x] 6 个方法签名正确

---

## 3.2 LocalFileSource 实现

### 执行目标
使用 `dart:io` 实现 `FileSource` 接口的本地文件系统版本。

### 任务边界
- `listDirectory` 返回 `List<FileEntry>`（文件夹优先 + 文件名升序）
- `streamFile` 用于大文件流式读取
- `testConnection` 检查路径是否存在
- `disconnect` 为空操作（本地无连接）

### 产出物
| 文件 | 说明 |
|------|------|
| `lib/shared/file_source/local_file_source.dart` | `LocalFileSource` |

### 验收标准
- [x] `listDirectory` 返回结果：文件夹在前，文件在后，按名称字母升序
- [x] `FileEntry` 对象含 name/isDirectory/size/modified
- [x] 不兼容文件类型不显示（仅展示文件夹 + 图片/PDF/视频/压缩包）
- [x] 空目录返回空列表
- [x] 单元测试：使用系统临时目录创建测试文件结构验证

---

## 3.3 FileSourceFactory

### 执行目标
实现工厂类，按 Source 类型创建对应 FileSource 实现，缓存活跃连接。

### 任务边界
- 当前仅 `local` 类型
- 缓存逻辑：同一 sourceId 复用已有 FileSource

### 产出物
| 文件 | 说明 |
|------|------|
| `lib/shared/file_source/file_source_factory.dart` | `FileSourceFactory` |

### 验收标准
- [x] `create(source)` 按 `source.type` 路由到 `LocalFileSource`（后续阶段加 `SmbFileSource`）
- [x] 同一 sourceId 重复调用返回同一实例（缓存）
- [x] `disconnect(sourceId)` 清理缓存并关闭连接

---

## 3.4 ContentProvider 抽象接口

### 执行目标
定义 `ContentProvider` 抽象接口，统一图片/PDF/压缩包的翻页内容。

### 任务边界
- 仅接口定义，不实现

### 产出物
| 文件 | 说明 |
|------|------|
| `lib/shared/content_provider/content_provider.dart` | `abstract class ContentProvider` |

### 验收标准
- [x] 接口定义与 `@tech/02-架构设计.md` §4.2 一致
- [x] `pageCount` getter
- [x] `loadPage(int index)` 返回 `Uint8List`
- [x] `dispose()` 清理资源

---

## 3.5 ImageFolderProvider 实现

### 执行目标
实现本地图片文件夹的 `ContentProvider`，按文件名排序提供翻页。

### 任务边界
- 仅处理文件夹类型资源（`ResourceType.folder`）
- 过滤仅保留支持的图片格式（jpg/png/gif/webp/bmp）
- 加载失败不跳过，抛异常给上层

### 产出物
| 文件 | 说明 |
|------|------|
| `lib/shared/content_provider/image_folder_provider.dart` | `ImageFolderProvider` |

### 验收标准
- [x] `pageCount` = 文件夹内支持的图片数量
- [x] `loadPage(index)` 返回正确图片字节
- [x] 文件名自然排序（2 排在 10 前面）
- [x] 不支持的格式跳过
- [x] `dispose()` 清理文件句柄
- [x] 单元测试：使用系统临时目录中的测试图片验证

---

## 3.6 ThumbnailGenerator 接口

### 执行目标
定义 `ThumbnailGenerator` 抽象接口，按资源类型路由缩略图生成。

### 任务边界
- 仅接口定义

### 产出物
| 文件 | 说明 |
|------|------|
| `lib/shared/thumbnail/thumbnail_generator.dart` | `abstract class ThumbnailGenerator` |

### 验收标准
- [x] `generate(source, relativePath)` 返回可选缩略图路径
- [x] 接口与 `@tech/02-架构设计.md` §4.4 一致

---

## 3.7 ImageThumbnailGenerator 实现

### 执行目标
使用 `image` 包解码并缩放图片为竖版缩略图（2:3 比例）。

### 任务边界
- 取资源文件夹内第一张图片
- 缩放至约 2:3 竖版尺寸，按显示尺寸解码

### 产出物
| 文件 | 说明 |
|------|------|
| `lib/shared/thumbnail/image_thumbnail_generator.dart` | `ImageThumbnailGenerator` |

### 验收标准
- [x] 缩略图比例约 2:3（竖长方形）
- [x] 按 `cacheWidth: 180, cacheHeight: 270` 尺寸解码
- [x] 生成后写入 `thumb_{resourceId}.jpg` 文件
- [x] 单元测试：给定测试图片，验证输出尺寸和比例

---

## 3.8 VideoThumbnailGenerator 实现

### 执行目标
使用 `media_kit` 截取视频首帧作为缩略图。

### 任务边界
- 仅为视频文件生成缩略图
- 截图失败时返回 null（使用占位图）

### 产出物
| 文件 | 说明 |
|------|------|
| `lib/shared/thumbnail/video_thumbnail_generator.dart` | `VideoThumbnailGenerator` |

### 验收标准
- [x] `screenshot()` 截取视频第一帧
- [x] 缩放至缩略图尺寸
- [x] 失败时返回 null 不抛异常

---

## 3.9 ThumbnailCacheService（LRU 磁盘缓存）

### 执行目标
实现缩略图磁盘 LRU 缓存，容量上限可配置，默认 500MB。

### 任务边界
- 磁盘存储 + JSON 索引文件记录访问时间
- 自动淘汰：写入时检查总大小，超限按 LRU 删除
- 手动清理：删除所有缩略图文件

### 产出物
| 文件 | 说明 |
|------|------|
| `lib/data/services/thumbnail_cache_service.dart` | `ThumbnailCacheService` |

### 验收标准
- [x] 缩略图文件命名 `thumb_{resourceId}.jpg`，存储在 `path_provider` 缓存目录
- [x] 默认容量上限 500MB
- [x] `getSize()/getCapacity()/setCapacity(int)/clearCache()` 方法
- [x] 写入时自动检查并淘汰最旧文件
- [x] 最小容量 500MB，拒绝更小值
- [x] 单元测试：写入超容后验证旧文件被清理

---

## 3.10 源管理 Repository（SourceRepository）

### 执行目标
实现 Source 的 CRUD 操作和 FileSource 连接调度。

### 任务边界
- 所有方法返回 `Result<T>`
- 配合 FileSourceFactory 管理连接生命周期

### 产出物
| 文件 | 说明 |
|------|------|
| `lib/data/repositories/source_repository.dart` | `SourceRepository` |

### 验收标准
- [x] `addSource(source)` — 插入 Source 记录，成功返回 `Ok(source)`
- [x] `getSources()` — 返回所有源列表
- [x] `deleteSource(id)` — 级联删除 Source + Resource + ResourceTag + 缩略图缓存 + disconnect
- [x] `toggleSource(id)` — 切换 enabled 状态
- [x] `markUnavailable(id)` / `markAvailable(id)` — 标记源可达状态
- [x] 全部方法错误包装为 `Result<T>`
- [x] 单元测试：drift 内存库 + CRUD 全路径

---

## 3.11 资源管理 Repository（ResourceRepository）

### 执行目标
实现 Resource 的 CRUD、去重 upsert、Stream 查询。

### 任务边界
- 键集分页支持
- Stream 查询驱动首页实时更新

### 产出物
| 文件 | 说明 |
|------|------|
| `lib/data/repositories/resource_repository.dart` | `ResourceRepository` |

### 验收标准
- [x] `addResource(resource)` — upsert（冲突列 sourceId+relativePath）
- [x] `getResources()` — 返回全量（P0）
- [x] `watchAvailableResources()` — Stream 仅返回 enabled+available 源的资源
- [x] `getResource(id)` — 单个查询
- [x] `deleteResource(id)` — 级联删除 ResourceTag
- [x] `updateResource(resource)` — 更新元数据
- [x] 全部方法错误包装为 `Result<T>`
- [x] 单元测试：级联删除 + unique 约束 + upsert 验证

---

## 3.12 文件系统 Repository（FilesystemRepository）

### 执行目标
封装文件浏览的委托逻辑，统一通过 FileSourceFactory 访问文件。

### 任务边界
- 按 `@tech/03-性能优化设计.md` §6 实现 TTL 缓存 + 请求去重

### 产出物
| 文件 | 说明 |
|------|------|
| `lib/data/repositories/filesystem_repository.dart` | `FilesystemRepository` + `DirectoryCache` |

### 验收标准
- [x] `listDirectory(sourceId, path)` — 通过 FileSourceFactory 委托，返回 `Result<List<FileEntry>>`
- [x] TTL 缓存：local 30s / SMB 2min
- [x] 请求去重：同一路径并发请求共享一个 Future
- [x] `invalidateCache(sourceId)` — 清理指定源全部缓存
- [x] 单元测试：mock LocalFileSource 验证缓存命中/过期

---

## 3.13 缩略图 Repository（ThumbnailRepository）

### 执行目标
封装缩略图生成和缓存管理。

### 任务边界
- 委托 ThumbnailGenerator 和 ThumbnailCacheService
- 在 Isolate 中生成缩略图

### 产出物
| 文件 | 说明 |
|------|------|
| `lib/data/repositories/thumbnail_repository.dart` | `ThumbnailRepository` |

### 验收标准
- [x] `generate(source, relativePath, resourceType)` — 按类型路由生成器
- [x] `get(resourceId)` — 从缓存取缩略图路径
- [x] `delete(resourceId)` — 删除指定缩略图
- [x] 全部方法错误包装为 `Result<T>`

---

## 3.14 源列表页（SourceListPage）

### 执行目标
实现数据源列表页：源列表展示（图标 + 名称 + 类型 + 资源数）、添加本地源按钮、添加 SMB 按钮（空入口）、删除源确认。

### 任务边界
- SMB 按钮暂时为占位（阶段 05 实现）
- 添加本地源：调用系统文件夹选择器 → 创建 Source 记录

### 产出物
| 文件 | 说明 |
|------|------|
| `lib/ui/features/sources/source_list_page.dart` | SourceListPage |
| `lib/ui/features/sources/view_models/source_list_view_model.dart` | SourceListViewModel |
| `lib/ui/features/sources/widgets/source_card.dart` | SourceCard Widget |

### 验收标准
- [x] 源列表项显示：类型图标 + 源名称 + 资源数量 + 状态指示（正常/不可用）
- [x] FAB [+] → 弹出菜单："添加本地文件夹" / "添加 SMB 网络共享"
- [x] 点击"添加本地文件夹" → 调用 `file_picker` 选择文件夹 → 创建 Source
- [x] 开关控制源启用/禁用
- [x] …菜单：重命名 / 删除（二次确认弹窗）
- [x] 删除后级联清除资源 + 缩略图
- [x] 空状态："还没有数据源，点击 + 添加"
- [x] 与 `@design/source-list.html` 布局一致

---

## 3.15 文件浏览器 — 列表视图

### 执行目标
实现文件浏览器的列表视图：目录导航、面包屑、文件夹/文件列表。

### 任务边界
- 仅列表视图
- 正常模式交互（非多选、非宽屏双栏）

### 产出物
| 文件 | 说明 |
|------|------|
| `lib/ui/features/sources/file_browser_page.dart` | FileBrowserPage |
| `lib/ui/features/sources/view_models/file_browser_view_model.dart` | FileBrowserViewModel |
| `lib/ui/features/sources/widgets/file_list_view.dart` | FileListView |

### 验收标准
- [x] 顶部：返回按钮 + 源名称 + 视图切换按钮
- [x] 面包屑导航：`📁 > Comics > 海贼王` 各层级可点击跳转
- [x] 文件列表：图标（文件夹/图片/PDF/视频）+ 名称
- [x] 点击文件夹 → 进入子目录
- [x] 点击图片/PDF/GIF → 进入查看器（3.18）
- [x] 点击视频 → 进入视频播放器（3.19）
- [x] 返回键逐级回退
- [x] 空目录："此文件夹为空"
- [x] 加载状态：骨架屏
- [x] 与 `@design/file-browser.html` 列表模式布局一致

---

## 3.16 文件浏览器 — 网格视图

### 执行目标
实现网格视图模式，文件夹取子文件夹内第一张图作为封面缩略图。

### 任务边界
- 与列表视图共享同一 ViewModel，仅切换显示模式
- 文件夹封面取第一张兼容文件缩略图

### 产出物
| 文件 | 说明 |
|------|------|
| `lib/ui/features/sources/widgets/file_grid_view.dart` | FileGridView |

### 验收标准
- [x] 切换按钮 `[≡]` 列表 / `[🔲]` 网格互切
- [x] 文件夹显示封面缩略图 + 名称
- [x] 文件显示自身缩略图 + 名称
- [x] 点击行为与列表视图一致
- [x] 与 `@design/file-browser.html` 网格模式布局一致

---

## 3.17 文件浏览器 — 多选模式

### 执行目标
实现多选模式：仅当前层级勾选、批量添加资源、批量打标签入口。

### 任务边界
- 多选不走 ResourcePicker（深层递归选择走 ResourcePicker，阶段 06 实现）
- 批量打标签入口暂时显示但不可用（阶段 04 实现）

### 产出物
| 文件 | 说明 |
|------|------|
| `lib/ui/features/sources/widgets/file_list_view.dart` | 多选模式状态扩展 |
| `lib/ui/features/sources/widgets/file_grid_view.dart` | 多选模式状态扩展 |

### 验收标准
- [x] 右上角 [多选] 按钮进入，[取消] 退出
- [x] 点击项切换勾选（复选框）
- [x] [全选] 勾选当前目录全部项
- [x] 顶部栏："已选 N 项"
- [x] 底部操作栏：[批量添加资源] [批量打标签] [取消]
- [x] 批量添加流程：过滤已入库/空文件夹 → 确认弹窗 → 创建 Resource → 生成缩略图
- [x] 添加后自动判定组织模式（阶段 06 实现判定逻辑，当前默认 direct）
- [x] 与 `@design/file-browser.html` 多选交互一致

---

## 3.18 图片查看器

### 执行目标
实现全屏图片查看器：单页左右翻页、缩放、工具栏显隐、滑动条快速定位、加载失败阻断。

### 任务边界
- 使用 `extended_image` 实现翻页 + 手势缩放
- 预加载窗口：当前页 + 前 1 + 后 2

### 产出物
| 文件 | 说明 |
|------|------|
| `lib/ui/features/viewer/viewer_page.dart` | ViewerPage |
| `lib/ui/features/viewer/view_models/viewer_view_model.dart` | ViewerViewModel |
| `lib/ui/features/viewer/widgets/slide_bar.dart` | SlideBar |
| `lib/ui/features/viewer/widgets/viewer_toolbar.dart` | ViewerToolbar |
| `lib/shared/content_provider/page_image_provider.dart` | `PageImageProvider`（稳定键 ImageProvider） |

### 验收标准
- [x] 全屏黑色背景，图片居中缩放适配
- [x] 左右滑动 / 点击右侧 → 下一页；点击左侧 → 上一页
- [x] 键盘 ← → 翻页，ESC 返回
- [x] 双击切换缩放（1x / 适应屏幕 / 2x）
- [x] 双指捏合 / 滚轮缩放（0.5x ~ 5x），缩放后可平移
- [x] **单击切换工具栏+滑动条显隐**
- [x] 工具栏：返回 + 资源名 + 当前页/总页数 + 设置按钮
- [x] 滑动条：底部横向滑块，拖动跳页，两端标注页码
- [x] 总页数 = 1 时不显示滑动条
- [x] 预加载窗口：进入时预加载 4 页，翻页平滑
- [x] 加载失败：显示"加载失败"占位 + 重试按钮，**不自动跳过**
- [x] `PageImageProvider` 使用 `(resourceId, pageIndex)` 稳定键（非 `Uint8List`）
- [x] `ContentProvider.dispose()` 在退出时清理所有资源
- [x] 与 `@design/viewer.html` 单页模式交互一致
- [x] Widget 测试：使用 1x1 最小 JPEG 字节验证翻页/工具栏显隐

---

## 3.19 统一查看器 — 视频支持

### 执行目标
在统一 ViewerPage 内添加视频渲染能力：图片/PDF/视频混合浏览，左右切换无缝。视频页由 ViewerViewModel 直接管理 media_kit Player（不经 ContentProvider）。

### 任务边界
- 使用 `media_kit` 系列
- ViewerViewModel 按当前 ViewerItem 类型分发：ImagePageItem/PdfPageItem → ContentProvider，VideoItem → media_kit Player
- 视频页不参与预加载窗口和双页模式
- 进度条可见/隐藏切换，上部切页/下部拖动进度条分区

### 产出物
| 文件 | 说明 |
|------|------|
| `lib/ui/features/viewer/widgets/video_player.dart` | VideoPlayer widget（media_kit 渲染） |
| `lib/ui/features/viewer/widgets/video_progress_bar.dart` | 视频进度条组件 |

### 验收标准
- [x] ViewerViewModel 渲染分发：ImagePageItem/PdfPageItem → ExtendedImage，VideoItem → VideoPlayer
- [x] 单击切换工具栏 + 进度条显隐
- [x] 双击视频画面播放/暂停
- [x] 进度条可见时：上部 70% 左右滑动切页，下部 30% 拖动进度条
- [x] 进度条隐藏时：全屏左右滑动切页（不触发视频内快进快退）
- [x] 长按进入倍速模式，默认 2.0x
- [x] 长按期间上下滑动切换倍速（1.0x ~ 3.0x，档位吸附）
- [x] 视频解码失败：显示占位 + 重试
- [x] 视频页不参与双页并排（自动回退单页）
- [x] 与 `@design/viewer.html` 视频模式交互一致

---

## 3.20 首页缩略图网格

### 执行目标
实现资源库首页的响应式缩略图网格、空状态、加载状态。

### 任务边界
- 使用 `GridView.builder` 实现虚拟化
- 缩略图按 2:3 竖版显示，文件名下方显示
- 资源类型角标（PDF/压缩包）
- 筛选栏暂不实现（阶段 04）
- 排序暂按添加时间倒序

### 产出物
| 文件 | 说明 |
|------|------|
| `lib/ui/features/home/home_page.dart` | HomePage（替换占位） |
| `lib/ui/features/home/view_models/home_view_model.dart` | HomeViewModel |
| `lib/ui/features/home/widgets/resource_grid.dart` | ResourceGrid |
| `lib/ui/features/home/widgets/resource_grid_item.dart` | ResourceGridItem |

### 验收标准
- [x] 响应式列数：<600dp 三列 / 600~900 四列 / 900~1200 五列 / >1200 六列
- [x] GridView.builder + `SliverGridDelegateWithMaxCrossAxisExtent(maxCrossAxisExtent: 180)`
- [x] `childAspectRatio: 2/3`
- [x] `cacheExtent: 1500`
- [x] 缩略图：`Image.file(cacheWidth: 180, cacheHeight: 270, fit: BoxFit.cover)`
- [x] 文件名最多 2 行，溢出省略号，12sp
- [x] PDF 和压缩包显示类型角标（右下角图标）
- [x] 文件夹无角标
- [x] 单击资源 → 跳转查看器 `/viewer/:resourceId`
- [x] 空状态："还没有资源" + "去添加数据源" 按钮 → 跳转 Tab 1
- [x] 加载状态：顶部扫描进度条，已扫描数量实时更新
- [x] 缩略图加载失败 → 显示类型占位图
- [x] Tab 切回时保持滚动位置（`PageStorageKey`）
- [x] 与 `@design/homepage.html` 布局一致
- [x] Widget 测试：空状态渲染、网格列数响应式

---

## 3.21 宽屏双栏文件浏览器

### 执行目标
实现 ≥900dp 宽屏模式下文件浏览器的左侧目录树 + 右侧内容区布局。

### 任务边界
- 与 `@design/file-browser.html` 宽屏模式布局一致
- 左侧目录树纯导航，右侧内容区复用 3.15/3.16 的列表/网格视图

### 产出物
| 文件 | 说明 |
|------|------|
| `lib/ui/features/sources/widgets/directory_tree.dart` | DirectoryTree 组件 |
| `lib/ui/features/sources/file_browser_page.dart` | 宽屏双栏布局适配 |

### 验收标准
- [x] 宽度 ≥ 900dp：左侧目录树（展开/折叠）+ 右侧内容区
- [x] 左侧目录树点击节点 → 右侧内容区更新
- [x] 左侧树当前选中节点高亮
- [x] 右侧面包屑导航独立
- [x] 右侧切换按钮 `[≡]`/`[🔲]` 仅控制右侧
- [x] 宽度 < 900dp：回退为 3.15/3.16 的全屏单栏模式
- [x] 与 `@design/file-browser.html` 宽屏布局一致

---

## 3.22 文件浏览器来源查看器（D9）

### 执行目标
实现从文件浏览器进入查看器的独立浏览模式：翻页范围 = 当前目录同层级兼容文件，工具栏标题 = 文件名+序号。

### 任务边界
- 路由 `/viewer/file/:sourceId`（已在阶段 01 §1.4 注册）
- 复用 ViewerPage，但传入不同的上下文参数（fileSource 而非 resource）

### 产出物
| 文件 | 说明 |
|------|------|
| `lib/ui/features/viewer/viewer_page.dart` | 扩展支持 fileSource 模式 |
| `lib/ui/features/viewer/view_models/viewer_view_model.dart` | 扩展支持文件级初始化 |

### 验收标准
- [x] 路由 `/viewer/file/:sourceId` 可正常跳转
- [x] 翻页范围 = 当前目录同层级兼容文件（图片/PDF/视频）
- [x] 工具栏标题 = 文件名 + 当前序号/总数
- [x] 左右滑动在同层级文件中切换，不跨界浏览父/子目录
- [x] 退出 → 返回文件浏览器，恢复之前位置和状态
- [x] 滑动条、缩放、加载失败阻断与资源查看器一致
- [x] 与 `@prd/04-资源查看器.md` §4.2-4.3 行为一致
